# see https://buildroot.org/downloads/manual/manual.pdf

which sed
make --version # > 3.81
# binutils
# build-essential
gcc --version #(version 4.4 or any later)
g++ --version #(version 4.4 or any later)
which bash
which patch
which gzip
which bzip2
which perl #(version 5.8.7 or any later)
which tar
which cpio
python --version #(version 2.6 or any later)
which unzip
which rsync
which file #(must be in /usr/bin/file)
which bc